@model IEnumerable<PP4.MVC.Models.ViewPelicula>
@{
    ViewBag.Title = "Datos Pelicula";
}
<h2>@ViewBag.Title.</h2>
<div class="row" style="text-align:left;padding:3px">
    <a href="~/Pelicula/Nuevo" class="btn btn-outline-primary">Nuevo</a>

</div>

<div class=" row">
    <div class="col-md-12">
        <table class="table">
            <thead class="thead-dark">

                <tr>
                    <th>ID</th>
                    <th>Nombre de Pelicula</th>
                    <th>Duracion</th>
                    <th>Disponibilidad</th>
                    <th>Horario</th>
                    <th>Sala</th>
                </tr>

                @foreach (var oElemento in Model)
                {
                    <tr>
                        <td>@oElemento.ID_Pelicula</td>
                        <td>@oElemento.Descripcion_Pelicula</td>
                        <td>@oElemento.Duracion</td>
                        <td>@oElemento.Estado</td>
                        <td>@oElemento.horario</td>
                        <td>@oElemento.ID_sala</td>

                        <td>
                            <a class="btn btn-outline-info" href="~/Pelicula/Editar/@oElemento.ID_Pelicula">Editar</a>
                            <a href="~/Pelicula/Eliminar/@oElemento.ID_Pelicula" class="btn btn-outline-danger" role="button">Eliminar</a>
                        </td>
                    </tr>
                }
        </table>
    </div>
</div>


